# 8. Функции: Твои персональные роботы-помощники

![Картинка функции](covers/cover_8_functions.png)

### 1. Что такое функции и зачем они нужны?
<!--- Функции — это основа модульного программирования. Без них код превращается в нечитаемую «спагетти» --->

Представь, что ты строишь огромный [город](../../3.2 healthy lifestyle/how to act in a dangerous situation/articles/lost-in-city.md) из LEGO. Если ты будешь каждый раз заново искать детали и собирать одну и ту же маленькую машинку с нуля, ты очень быстро устанешь. Намного круче иметь «волшебную кнопку»: нажал её — и готовая машинка появилась на столе!

В программировании такая «кнопка» называется **Функцией**. 

**Функция** — это отдельный кусочек кода, у которого есть имя. Ты один раз записываешь в него инструкции (например, «как испечь блин»), а потом просто называешь его по имени, когда тебе это нужно.

**Главные плюсы функций:**
1.  **[Порядок](../../1.2_natural_sciences/physics_in_everyday_life/Q45003.md):** Твой [код](1_introduction.md) не выглядит как бесконечный свиток. Он разбит на аккуратные [блоки](../../1.2_natural_sciences/physics_in_everyday_life/Q169019.md).
2.  **Повторное использование:** Написал функцию один раз — используй хоть миллион раз!
3.  **Легкое исправление:** Если ты нашел ошибку в «рецепте блинчиков», тебе нужно исправить её только в одном месте (в функции), а не во всей программе.

---

### 2. Как создать свою функцию? (Объявление и [Определение](../../1.2_natural_sciences/physics_in_everyday_life/Q29996.md))

Создание функции похоже на наем сотрудника на [работу](../../8.2_future/choosing_a_career_path/articles/interview.md). Сначала ты должен решить, как его зовут и что он будет делать.

В [C](../../2.1_society/how_and_where_find_friends/articles/sora_drug.md)++ это выглядит так:

```cpp
тип_возвращаемого_значения имя_функции(параметры) {
    // Здесь живут команды (тело функции)
}
```

Давай разберем это на примере **Робота-Повара**:

```cpp
void makeToast() {
    std::cout << "Берем хлеб..." << std::endl;
    std::cout << "Кладем в тостер..." << std::endl;
    std::cout << "Хрум! Тост готов!" << std::endl;
}
```

*   **`void`** — это «[пустота](../../1.1_structure_of_the_world/matter/articles/02_atomism.md)». Это значит, что наш робот просто делает работу и ничего не возвращает нам в руки (не дает отчет или число).
*   **`makeToast`** — это имя. Мы сами его придумали.
*   **`()`** — это пустые «карманы». Сюда мы могли бы положить что-то для [работы](../../8.2_future/choosing_a_career_path/articles/interview.md) (например, варенье), но сейчас нам ничего не нужно.
*   **`{ }`** — это стены комнаты, в которой работает наш робот.

---

### 3. Аргументы: Даем роботу [инструменты](../../1.2_natural_sciences/physics_in_everyday_life/Q36253.md)
<!--- Передача аргументов позволяет делать функции гибкими и универсальными --->

Если наш робот умеет делать только один вид тостов — это скучно. Давай научим его принимать «заказы». То, что мы даем функции на вход, называется **Аргументами** или **Параметрами**.

Представь, что аргументы — это [ингредиенты](../../6.1_Independent_living_and_daily_living_skills/Simple_and_safe_cooking/articles/how_to_read_recipe.md) в корзинке, которую ты передаешь роботу.

```cpp
void makeSandwich(std::string ingredient) {
    std::cout << "Делаем бутерброд с: " << ingredient << std::endl;
}
```

Теперь, когда мы вызываем функцию, мы можем сказать:
`makeSandwich("Колбаса");`
или
`makeSandwich("Сыр");`

**[Аналогия](../../1.2_natural_sciences/physics_in_everyday_life/Q46344.md):** Это как пульт от телевизора. Кнопка одна (функция), но номер канала ([аргумент](../../5.1_technology_and_digital_literacy/information and media literacy/критическое_мышление_в_онлайн_среде.md)), который ты вводишь, меняет то, что ты увидишь на экране.

---

### 4. Возвращаемые значения: Робот приносит отчет
<!--- Оператор return — это способ функции передать результат своей работы обратно в основную программу --->

Иногда нам нужно, чтобы функция не просто что-то вывела на [экран](../../3.1. healthy lifestyle/Sleep, nutrition, and adolescent energy/articles/gadgets_blue_light_sleep.md), а принесла нам [результат](../../1.2_natural_sciences/why_science_help_understand_world/experimental_science.md). Например, посчитала пример. 

Представь **Робота-Математика**. Ты даешь ему два числа, а он возвращает тебе их сумму.

```cpp
int addNumbers(int a, int b) {
    int sum = a + b;
    return sum; // Робот отдает результат нам в руки!
}
```

*   **`int`** в самом начале — это [тип](13_struct.md) данных. Мы говорим: «Робот, ты должен вернуть нам целое число».
*   **`return`** — это самая важная [команда](../../4.1_rules_of_study/how_to_learn_effectively/articles/peer_learning.md). Она говорит функции: «Всё, [работа](../../1.2_natural_sciences/physics_in_everyday_life/Q11382.md) закончена, вот твой результат, забирай!».

> [!IMPORTANT]
> Как только компьютер доходит до слова `return`, он сразу выходит из функции. Все команды, которые написаны ПОСЛЕ `return`, выполняться не будут. Робот отдал заказ и ушел домой!

---

### 5. Прототипы: [Список](10_arrays.md) имен для компьютера
<!--- Прототипы функций нужны для того, чтобы компилятор знал о существовании функции до того, как она будет полностью описана --->

Компьютер читает твой код сверху вниз, как книгу. Если ты вызовешь функцию в начале файла, а опишешь её в самом конце, компьютер запаникует: «Я не знаю, кто такой `makeToast`! Его еще не было в книге!».

Чтобы этого не случилось, мы используем **Прототипы**. Это как «Меню» в ресторане. Официант дает тебе список блюд (прототипы) в начале, чтобы ты знал, что можно заказать, хотя сами блюда еще на кухне.

**Пример программы с прототипом:**

```cpp
#include <iostream>

// Вот наш прототип (Меню)
// Мы просто говорим компьютеру: "Такая функция будет!"
void sayHello(std::string name); 

int main() {
    sayHello("Ярик"); // Теперь компьютер спокоен, он видел имя в списке
    return 0;
}

// А вот здесь мы уже рассказываем, как именно функция работает
void sayHello(std::string name) {
    std::cout << "Привет, " << name << "! Как дела?" << std::endl;
}
```

---

### 6. Модульный код: Как LEGO-город
<!--- Модульность — это искусство разбиения сложной задачи на простые и понятные функции --->

Зачем всё это нужно? Чтобы твой код был **Модульным**. 

Представь игру «Майнкрафт». У неё миллионы строк кода. Если бы всё было в одном месте, программисты бы сошли с ума! Вместо этого у них есть тысячи маленьких функций:
*   `spawnZombie()` — создает зомби.
*   `breakBlock()` — ломает [блок](2_syntax.md).
*   `playMusic()` — включает музыку.

Каждый занимается своим делом. Это и есть **[модульность](8_functions.md)**. Твоя [программа](../../5.1_technology_and_digital_literacy/operating system/articles/process.md) — это команда специалистов, где каждый мастер в чем-то одном.

---

### 7. Большая шпаргалка по функциям

Чтобы ты не запутался, вот таблица-сравнение:

| Часть функции | Что это в реальной жизни? | Зачем это нужно? |
|:--------------|:--------------------------|:-----------------|
| **Имя**       | Название [кнопки](../../7.1_art/musical_instruments/articles/accordion.md)           | Чтобы знать, как вызвать помощника. |
| **Параметры** | Ингредиенты в корзинке    | Чтобы функция могла работать с разными данными. |
| **[Тело](../../1.2_natural_sciences/why_science_help_understand_world/organism.md) `{}`** | Стены лаборатории         | Место, где происходит вся магия и [расчеты](../../1.2_natural_sciences/physics_in_everyday_life/Q11023.md). |
| **[Return](8_functions.md)**    | Доставка курьером         | Чтобы отправить результат работы обратно. |
| **Void**      | Бесплатная [помощь](../../3.1_healthy_lifestyle/pervaya_pomoshch/ushibi_porezy_ozhogi/10_krovotechenie_chto_delat.md)         | Если нам не нужен результат, а просто [действие](../../2.1_society/cause_and_effect_relationships/articles/personal_choice.md). |

---

### 8. Полный пример: Супер-калькулятор урона
Давай напишем что-то посложнее, используя всё, что мы выучили!

```cpp
#include <iostream>

// Прототип функции
int calculateDamage(int attack, int defense);

int main() {
    int myAttack = 50;
    int enemyDefense = 20;

    // Вызываем функцию и сохраняем результат в переменную
    int finalDamage = calculateDamage(myAttack, enemyDefense);

    std::cout << "--- БОЙ НАЧАЛСЯ ---" << std::endl;
    std::cout << "Герой наносит " << finalDamage << " урона монстру!" << std::endl;

    if (finalDamage > 25) {
        std::cout << "Критический удар! Монстр в шоке!" << std::endl;
    }

    return 0;
}

// Определение функции
int calculateDamage(int attack, int defense) {
    int damage = attack - defense; // Простая математика

    // Если защита больше атаки, урон не может быть отрицательным!
    if (damage < 0) {
        damage = 0;
    }

    return damage; // Возвращаем итоговое число
}
```

---

### 9. [Советы](../../7.2 Media, leisure and hobbies /useful_and_interesting_leisure/articles/mistakes_in_choosing_hobby.md) для юного мастера функций

1.  **Одна функция — одна задача.** Не делай функцию «сварить суп, помыть пол и поиграть в приставку». Пусть это будут три разные функции. Так проще!
2.  **Давай понятные имена.** Вместо `f1()` или `aaa()` назови функцию `jump()` или `saveGame()`. Твой будущий «Я» скажет тебе спасибо через неделю.
3.  **Не делай функции слишком длинными.** Если функция не влезает на экран твоего монитора — скорее всего, её пора разбить на две маленькие.

> [!TIP]
> Помни: Функции — это твои рабы. Они делают только то, что ты им скажешь. Если функция работает неправильно, не ругай её, а проверь «[рецепт](../../6.1_Independent_living_and_daily_living_skills/Simple_and_safe_cooking/articles/how_to_read_recipe.md)» внутри фигурных скобок `{ }`!

---

### Ссылки на связанные темы
- Хочешь узнать, где именно «живут» переменные внутри функций? Читай [Раздел 9. Область видимости](./9_scopes.md).
- Чтобы понять, как функция вписывается в общий [файл](../../5.1_technology_and_digital_literacy/operating system/articles/file_system.md) программы, загляни в [Раздел 2. Структура программы](./2_syntax.md).

---
[Вернуться к оглавлению](./article_index_information_media_literacy.md)

---
Авторы: Иван Леу;  
*[Ресурсы](../../2.1_society/cause_and_effect_relationships/articles/ecological_footprint.md): [LLM](../../7.1_art/modern_technological_art/README.md) - Gemini*